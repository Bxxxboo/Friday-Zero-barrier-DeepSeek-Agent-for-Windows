---
description: 发布时同步版本号，并 push Gitee + GitHub 双远端
alwaysApply: true
---

# 版本号与 Gitee / GitHub 双端同步

每次向远端发布更新时，**必须**：

1. **bump 版本号**（`friday/version.py` + `scripts/version_info.py`）
2. **同时 push** `origin`（GitHub）与 `gitee`（Gitee）
3. **同时发 Release**（`Friday-Windows.zip`）：Gitee 必发（国内更新源），GitHub 可选（需 VPN/gh）

## semver

| 级别 | 何时用 |
|------|--------|
| **patch** | bug 修复、UI 微调、脚本/CI |
| **minor** | 新功能、较大 UI 重构（兼容） |
| **major** | 破坏性变更 |

## 一键发布（推荐）

```powershell
$env:GITEE_TOKEN='令牌'
scripts\publish-release.cmd -Bump patch -GitHubRepoName Friday-Zero-barrier-DeepSeek-Agent-for-Windows
```

步骤：sync GitHub+Gitee → Gitee Release → GitHub Release（有 gh 时）

## 仅同步代码（不发安装包）

```powershell
scripts\sync-remotes.cmd -Bump patch -GitHubRepoName Friday-Zero-barrier-DeepSeek-Agent-for-Windows
```

## 远端约定

| 远端 | 地址 |
|------|------|
| **origin** | `github.com/Bxxxboo/<GitHubRepoName>` |
| **gitee** | `gitee.com/Bxxxboo/friday` |

## 禁止

- 只 push GitHub 不 push Gitee
- 只发 Gitee Release 不 bump 版本
- 功能更新 push 时不改 `friday/version.py`

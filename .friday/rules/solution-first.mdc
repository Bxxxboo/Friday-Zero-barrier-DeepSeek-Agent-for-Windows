---
description: 已迁移 —— 全局规则见 ~/.cursor/rules/solution-first.mdc；仓库副本见 .cursor/rules/solution-first.mdc
alwaysApply: false
---

# 先方案、后改码（全局规则）

本文件不再作为生效入口。请使用：

| 范围 | 路径 |
|------|------|
| **所有 Cursor 项目（全局）** | `C:\Users\<用户名>\.cursor\rules\solution-first.mdc` |
| **本仓库（团队共享）** | `.cursor/rules/solution-first.mdc` |
| **星期五桌面 Agent** | 内置规则 `先方案、后改码（solution-first）`（`friday/rules.py`） |

完整正文以 `.cursor/rules/solution-first.mdc` 为准。

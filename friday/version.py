"""应用版本号 —— 与 PyInstaller version_info 保持一致。"""

__version__ = "1.0.1"
__version_tuple__ = (1, 0, 1, 0)

# GitHub Releases 更新源（owner/repo）。环境变量 FRIDAY_GITHUB_REPO 可覆盖。
GITHUB_REPO = "Bxxxboo/friday"
GITHUB_HOME = f"https://github.com/{GITHUB_REPO}"
